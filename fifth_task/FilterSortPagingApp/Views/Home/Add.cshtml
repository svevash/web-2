@model FilterSortPagingApp.Models.Toy

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>New toy:</h2>

@using (Html.BeginForm("Add", "Home", FormMethod.Post))
{
    @Html.LabelFor(model => model.Name, "Name")<br/>
    @Html.EditorFor(model => model.Name)<br/>
    @Html.LabelFor(model => model.Type.Name, "Type")<br/>
    @Html.EditorFor(model => model.Type.Name)<br/>
    @Html.LabelFor(model => model.Price, "Price")<br/>
    @Html.EditorFor(model => model.Price)<br/>
    <br/>
    <input type="submit" value="Add" />
}